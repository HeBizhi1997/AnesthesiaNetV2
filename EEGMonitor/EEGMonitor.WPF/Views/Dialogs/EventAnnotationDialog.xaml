<Window x:Class="EEGMonitor.Views.Dialogs.EventAnnotationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Clinical Event" Height="300" Width="420"
        Background="#161B22" Foreground="#E6EDF3"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Event Type" FontSize="11" Foreground="#8B949E" Margin="0,0,0,4"/>
        <ComboBox Grid.Row="1" x:Name="EventTypeCombo"
                  Background="#21262D" Foreground="#E6EDF3"
                  BorderBrush="#30363D" Height="28" FontSize="11" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="2" Text="Label" FontSize="11" Foreground="#8B949E" Margin="0,0,0,4"/>
        <TextBox Grid.Row="3" x:Name="LabelBox"
                 Background="#21262D" Foreground="#E6EDF3"
                 BorderBrush="#30363D" Height="28" FontSize="11" Padding="6,4" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="4" Text="Notes (optional)" FontSize="11" Foreground="#8B949E" Margin="0,0,0,4"/>
        <TextBox Grid.Row="4" x:Name="NotesBox"
                 Background="#21262D" Foreground="#E6EDF3"
                 BorderBrush="#30363D" FontSize="11" Padding="6,4"
                 TextWrapping="Wrap" AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 Margin="0,20,0,0"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancel" Click="CancelClick"
                    Background="#21262D" Foreground="#E6EDF3"
                    BorderBrush="#30363D" Padding="16,6" Margin="0,0,8,0" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Content="Add Event" Click="AddClick"
                    Background="#238636" Foreground="White"
                    BorderBrush="#2EA043" Padding="16,6" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Grid>
</Window>
